extends ../layout

block content
  .container
    h1.main-title 파티 수정
    form(action=`/questions/${question.id}`, method="post")
      input(type="hidden", name="_method", value="PUT")
      include _form
      button.btn.btn-outline-primary(type="submit") 수정 사항 적용
